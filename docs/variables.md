# Variables

Variables are defined with the `var` keyword. Since Conan is statically typed, each variable initialization must declare the variable's type

```
var a: i32 = 54;
var b: i32 = a + b;
var c: f32 = 3.14;
```

You can assign expressions to variables.

## Next chapter

You can go to the [next chapter](expressions.md), or you can return to the [table of contents](index.md).